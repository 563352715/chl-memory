# CHL Frontend Tab + Header-Button Audit — 2026-05-10

**Mandate:** stop accidentally creating duplicate tabs. Read-only audit of `frontend/src/App.js`, view files, and route paths. After this audit the next agent will remove confirmed duplicates and lock in a canonical layout.

**Source of truth:** `C:\CHL\frontend\src\App.js`
- Tab nav array: lines 2914-2991
- Header buttons cluster: lines 2782-2894
- Path-based route dispatch (`/ops`, `/sop-flow`, `/prevet`, etc.): lines 18171-18241

**Verdict at a glance.** Six confirmed duplicates / overlapping surfaces, all introduced today (2026-05-10) or as Iter 135.13 "header pill -> top-bar promotion". Recommendation: keep header pills, **remove the five `_route_*` tabs**; remove **BOL Search tab** (subsumed by Ops Console v2 + global search); keep **Ops Console v2** (button-driven, Playwright-drivable) and remove the legacy `_route_ops` tab. Drops nav from ~37 entries down to ~30.

---

## 1. Master inventory

### 1A. Tab nav (App.js lines 2914-2991)

| id | label | icon | view file / function | category | one-line description |
|---|---|---|---|---|---|
| dashboard | Dashboard | ChartLine | `views/DashboardView.jsx` | OPERATIONS | Top-level KPI dashboard (loads, P&L, alerts) |
| day1_dashboard | Day-1 Monitor | Gauge | `views/Day1Dashboard.jsx` | OPERATIONS | Owner-only launch-day single-page operator monitor (`/api/dashboard/day1`) |
| command | Command Phone | PhoneIcon | `views/CommandPhoneView.jsx` | COMMS | Voice + SMS unified console (Twilio/Plivo) |
| monitoring | Monitoring | Eye | `MonitoringView` in App.js | OPERATIONS | System health + telemetry monitoring |
| loads | Load Board | Package | `LoadsView` in App.js (line 3528) | OPERATIONS | CHL-internal loads CRUD (post / filter / open dossier) |
| matches | AI Matching | Lightning | `MatchesView` + `PendingCarrierRequests` | OPERATIONS | AI carrier-to-load matching surface |
| carriers | Carriers DB | IdentificationCard | `CarriersView` in App.js (line 6757) | CARRIER MGMT | Carrier registry: filter / search / open carrier card |
| market_radar | Market Radar | Crosshair | `views/MarketRadar.jsx` | FINDING WORK | Single-pane unified hunt-and-close view (lazy-loaded) |
| boards | Loadboard Connections | Briefcase | `LoadBoardsManagerView` (line 8384) | FINDING WORK | Manage credentials/sources for external load boards (DAT, 123LB, Truckstop, etc.) |
| rates | Market Rates | TrendUp | `MarketRatesView` (line 8190) | FINDING WORK | Lane-rate analytics (`/api/market-rates`) |
| rules | Auto Rules | SlidersHorizontal | `AutoRulesView` | OPERATIONS | Autopilot rule editor |
| live_map | Live Map | Crosshair | `views/LiveGpsMapView.jsx` + exception/check-in panels | OPERATIONS | GPS map + tracking exceptions + carrier check-ins |
| inbox | Inbox | ChatCircleDots | `InboxView` | COMMS | dispatch@continentalhaul.com inbox (`/api/_inbox/list`) |
| aggregator | Find Loads | Globe | `views/LoadBoardAggregator.jsx` | FINDING WORK | Friendly multi-source aggregator (CHL Direct + SAM.gov + paid sources) |
| bulk_hunter | Specialty Hauler Lists | Target | `HaulerCategoriesView` (line 12335) | CARRIER MGMT | 4-category specialty-hauler outreach (hopper, livestock, tanker, protein) |
| factor_api | Factors | Plug | `views/FactorAPIWiringView.jsx` | FINANCIAL | Per-factor encrypted credential wiring + push-to-factor |
| disputes | Disputes | Shield | `DisputesDashboardView` (line 13071) | FINANCIAL | Factor disputes + clawback events (`/api/factoring/disputes`) — owner-only |
| patch_proposals | Patch Proposals | Robot | `PatchProposalsDashboardView` (line 13280) | SELF-HEAL | sub_agent_dispatcher proposals (`/api/_self_healing/patch_proposals`) — owner-only |
| outcomes | Outcomes | TrendUp | `OutcomesDashboardView` (line 14726) | SELF-HEAL | outcome_tracker + weight_tuning_proposer metrics — owner-only |
| pipeline_walkthrough | Pipeline | ChartLine | `PipelineWalkthroughView` | OPERATIONS | RFQ -> Pay full-lifecycle walkthrough — owner-only |
| renewals | Renewals | CalendarCheck | `RenewalsView` (line 15828) | GOVERNANCE | Authority/insurance/bond expiration tracker (`/api/_renewals/summary`) — owner-only |
| self_heal | Self-Heal | ShieldCheck | `SelfHealView` (line 13498) | SELF-HEAL | Observability log of every self-heal action (`/api/_self_heal/recent`) — owner-only |
| dev_reviews | Reviews | ListChecks | `DevReviewQueueView` (line 14133) | SELF-HEAL | YELLOW-tier patches awaiting operator approve/reject — owner-only |
| urgent_alerts | Alerts | Warning | `UrgentAlertsView` (line 14457) | SELF-HEAL | RED-tier alerts from llm_reasoning_loop + inbox_triage_scanner — owner-only |
| ops_console_new | Ops Console v2 | Wrench | `views/OpsConsoleView.jsx` | MANUAL OPS | Button-driven manual workflow deck (synth load, dry-run quote, BOL search, invoice+NOA, etc. — all `/api/ops_console/*`) — owner-only **[ADDED 2026-05-10]** |
| _route_ops | Ops Console | Eye | href `/ops` -> `views/OperatorConsoleView.jsx` | MANUAL OPS | New-tab link to live brokerage state console (`/ops` route) |
| _route_sopflow | SOP Flow | ClipboardText | href `/sop-flow` -> `views/SopFlowView.jsx` | OPERATIONS | New-tab link to load-lifecycle kanban |
| _route_prevet | Pre-Vet | IdentificationCard | href `/prevet` -> `views/CarrierPrevetView.jsx` | CARRIER MGMT | New-tab link to pre-vetting pipeline |
| _route_scorecard | Shipper Scorecard | Target | href `/shipper-scorecard` -> `views/ShipperScorecardView.jsx` | OPERATIONS | New-tab link to shipper scorecards |
| _route_failures | Failure Console | Shield | href `/failures` -> `views/FailureModeConsole.jsx` | SELF-HEAL | New-tab link to failure-mode flags (authority lost, GPS silent, no-show, factor decline) |
| sop_playbook | SOP Cards | ClipboardText | `views/SOPPlaybookView.jsx` | OPERATIONS | Two operating models (invoice/driver), printable narrative cards of SOP stages |
| accounting | Accounting | Calculator | `AccountingView` | FINANCIAL | Books / ledger / transactions |
| owner | Owner | Crown | `views/OwnerDashboard.jsx` | GOVERNANCE | Owner-only governance dashboard |
| doc_review | Doc Review | Shield | `views/ComplianceTab.jsx` | DOCUMENT / BOL | Pending-document review queue (`/api/compliance/pending-review`) |
| compliance | Compliance | Shield | `ComplianceView` (line 5752) | GOVERNANCE | FMCSA/authority checklist + resources (`/api/compliance/checklist`) |
| bol_search | BOL Search | MagnifyingGlass | `views/BolSearchView.jsx` | DOCUMENT / BOL | BOL-keyed full-document timeline (`/api/documents/by_bol/{bol}`) **[ADDED 2026-05-10]** |
| vault | Vault | Vault | `VaultView` | GOVERNANCE | Encrypted credential + document vault |
| operator_queues | Queues | ShieldWarning | `views/OperatorQueuesView.jsx` | OPERATIONS | Wave-1 review dashboard for 5 collections — owner-only |
| aether | AEther | Target | `SovereignPulseMonitor` | SELF-HEAL | Sovereign pulse monitor / self-heal heartbeat |
| notifications | Notif Log | EnvelopeSimple | `views/NotificationLogView.jsx` | COMMS | Outbound notification log — owner-only |
| factor_matrix | Factor Picker | HandCoins | `views/FactorMatrixView.jsx` | FINANCIAL | Ranked factoring-provider registry + OCR NOA test + push — owner-only |
| settings | Settings | Gear | `BusinessSettingsView` | GOVERNANCE | Business settings (SMS/email, monthly P&L email, etc.) |

**Tab count:** 30 always-visible + 12 owner-only = 42 if owner. Wider than expected.

### 1B. Header buttons (lines 2782-2894, right side of nav)

| Position (L->R) | Label | Style | Target | Same-as-tab? |
|---|---|---|---|---|
| 1 | Connection-status indicator | dot+label | n/a | display-only |
| 2 | Username pill | text | n/a | display-only |
| 3 | NotificationBell | bell icon dropdown | in-app navigate-to-tab callback | display-only |
| 4 | RequiredTasksTray | tray icon | in-app pop-out | display-only |
| 5 | Global search bar (`LoadDossierSearchBar`) | input | `/api/load-dossier/search` (loads by ref / city / shipper / driver / MC) | n/a (search-only) |
| 6 | **Calendar** | white pill | `<a href="/calendar" target="_blank">` -> `PlatformCalendarView` | NO direct tab. (Renewals tab is RELATED but distinct.) |
| 7 | **Drafts** | yellow pill | `<a href="/pending-docs" target="_blank">` -> `PendingDocsView` (drafts auto-generated on delivered loads) | NO matching tab |
| 8 | **Scorecards** | green pill | `<a href="/shipper-scorecard" target="_blank">` -> `ShipperScorecardView` | **YES: identical to `_route_scorecard` tab** |
| 9 | **Console** | sky pill | `<a href="/ops" target="_blank">` -> `OperatorConsoleView` | **YES: identical to `_route_ops` tab** |
| 10 | **SOP Flow** | indigo pill | `<a href="/sop-flow" target="_blank">` -> `SopFlowView` | **YES: identical to `_route_sopflow` tab** |
| 11 | **Pre-Vet** | amber pill | `<a href="/prevet" target="_blank">` -> `CarrierPrevetView` | **YES: identical to `_route_prevet` tab** |
| 12 | **Email** | yellow dropdown (`EmailMenuButton`) | Dropdown: **Create** = open inline compose modal (`/outbound/test-email`); **View** = `setActiveTab("inbox")` -> Inbox tab | "View" lands on Inbox tab; "Create" is unique (compose modal) |
| 13 | **Logout** | red pill | `onLogout()` callback | unique |

Header button-cluster construction sequence: `Calendar`, `Drafts`, `Scorecards`, `Console`, `SOP Flow`, `Pre-Vet`, separator, `Email` dropdown, `Logout`. Five of these duplicate `_route_*` tabs verbatim; one (`Email -> View`) routes to the Inbox tab; two (`Calendar`, `Drafts`) are header-only and have no matching tab.

---

## 2. Pair-by-pair findings

| # | Suspect A | Suspect B | Endpoint A | Endpoint B | Verdict | Recommendation |
|---|---|---|---|---|---|---|
| 1 | Header **Email** (yellow dropdown) | Tab **Inbox** | Create: `POST /outbound/test-email` (modal); View: `setActiveTab("inbox")` | `GET /api/_inbox/list` | **DISTINCT (with bridge)** | Email button's "View" item ROUTES TO the Inbox tab; it does not duplicate. Keep both. The yellow dropdown is the canonical "create or view email" entry point per EOD-16-late operator UX cleanup (line 2778 comment). |
| 2 | Tab **Ops Console** (`_route_ops`, href `/ops`) | Tab **Ops Console v2** (`ops_console_new`) | `OperatorConsoleView` (live state + agent journal, read-only-ish) | `OpsConsoleView` (button-driven `/api/ops_console/*` synth load / dry-run / BOL / invoice / rate-con) | **DISTINCT but operator flagged Ops Console v2 as the today-added duplicate** | Operator-stated rule says "for my new tabs, bias toward REMOVE". Two reasonable resolutions: (a) REMOVE `ops_console_new` (revert today's add) since the legacy `/ops` already exists; (b) REMOVE `_route_ops` tab (and the duplicate header Console pill — see #5) since Ops Console v2 is the Playwright-drivable surface that lives inside the SPA. **Recommend (b)**: keep Ops Console v2 (in-SPA, button-driven, owner-only) + keep the Header `Console` pill for the read-only `/ops` view. Remove the redundant `_route_ops` tab. |
| 3 | Tab **BOL Search** (`bol_search`, added 2026-05-10) | Global search bar (`LoadDossierSearchBar`) | `GET /api/documents/by_bol/{bol}` (full timeline: rate cons, BOLs, PODs, invoices, NOAs, vault, carrier docs, emails) | `GET /api/load-dossier/search?q=...` (load records: ref#, city, shipper, consignee, driver, MC) | **OVERLAPPING (different endpoints, overlapping inputs)** | The global bar's placeholder is literally "Search BOL/ref# · driver · shipper · city" so users will type BOLs there too -- but it returns LOADS, not the doc timeline. **Plus**, OpsConsoleView v2 (kept under #2) already has a "BOL Quick Search" card calling `/api/ops_console/bol_search` (which proxies the same `/api/documents/by_bol` data). **DUPLICATE** w/ Ops Console v2's BOL card. Operator: "for my new tabs, bias toward REMOVE". **Recommend REMOVE `bol_search` tab.** Functionality stays accessible via Ops Console v2 panel + global search bar. |
| 4 | Header **SOP Flow** pill | Tab **SOP Flow** (`_route_sopflow`) | href `/sop-flow` (new tab) -> `SopFlowView` | href `/sop-flow` (new tab) -> `SopFlowView` | **DUPLICATE — verbatim** | Both render the same page. Iter 135.13 promoted the header pill into the tab bar but the original header pill was never removed. **Recommend REMOVE `_route_sopflow` tab; keep header `SOP Flow` pill.** |
| 5 | Header **Console** pill | Tab **Ops Console** (`_route_ops`) | href `/ops` -> `OperatorConsoleView` | href `/ops` -> `OperatorConsoleView` | **DUPLICATE — verbatim** | Same target. **Recommend REMOVE `_route_ops` tab; keep header `Console` pill.** |
| 6 | Header **Pre-Vet** pill | Tab **Pre-Vet** (`_route_prevet`) | href `/prevet` -> `CarrierPrevetView` | href `/prevet` -> `CarrierPrevetView` | **DUPLICATE — verbatim** | **Recommend REMOVE `_route_prevet` tab; keep header `Pre-Vet` pill.** |
| 7 | Header **Scorecards** pill | Tab **Shipper Scorecard** (`_route_scorecard`) | href `/shipper-scorecard` -> `ShipperScorecardView` | href `/shipper-scorecard` -> `ShipperScorecardView` | **DUPLICATE — verbatim** | **Recommend REMOVE `_route_scorecard` tab; keep header `Scorecards` pill.** |
| 8 | Tab **Failure Console** (`_route_failures`) | (no matching header pill) | href `/failures` -> `FailureModeConsole` | n/a | **DISTINCT** (no duplicate) | Failure Console has no header pill. **Keep the tab** -- removing it would lose access to the failure-mode dashboard. |
| 9 | Tab **Load Board** (`loads`) | Tab **Find Loads** (`aggregator`) | `LoadsView` (CHL-internal loads CRUD: post / filter / open) | `LoadBoardAggregator` (multi-source aggregator: CHL Direct + SAM.gov + paid) | **DISTINCT** | Load Board = internal-load registry; Find Loads = external-source aggregator. Rename consideration: "Load Board" -> "My Loads" would reduce ambiguity, but they are functionally distinct. Keep both. |
| 10 | Tab **Load Board** (`loads`) | Tab **Loadboard Connections** (`boards`) | `LoadsView` | `LoadBoardsManagerView` (`/api/load-board-sources`: CRUD credentials for external boards) | **DISTINCT** | Loadboard Connections = SETUP page (credentials / API wiring); Load Board = ops surface. Keep both. |
| 11 | Tab **Find Loads** (`aggregator`) | Tab **Loadboard Connections** (`boards`) | `LoadBoardAggregator` (live load feed) | `LoadBoardsManagerView` (credentials manager) | **DISTINCT** | Different lifecycles (run vs configure). Keep both. |
| 12 | Tab **Market Radar** (`market_radar`) | Tab **Market Rates** (`rates`) | `MarketRadar.jsx` (single-pane unified hunt-and-close — opportunities, postings, hot lanes) | `MarketRatesView` (lane-rate analytics: rates per lane, trends, hot lanes; `/api/market-rates`) | **OVERLAPPING** | Both surface "hot lanes". Market Radar is the broader "find opportunity" pane; Market Rates is rate analytics. They overlap on hot-lanes data. **Recommend KEEP both for now** (different primary use); revisit at iter close after operator sees both in use. |
| 13 | Tab **Self-Heal** | Tab **Patch Proposals** | `/api/_self_heal/recent` (action LOG -- everything self-heal has done) | `/api/_self_healing/patch_proposals` (sub_agent_dispatcher PROPOSALS awaiting outcome) | **DISTINCT** | Log vs proposal queue. Keep both. |
| 14 | Tab **Outcomes** | Tab **Reviews** | outcome_tracker + weight_tuning_proposer (metrics on past actions) | `dev_review_queue` (YELLOW-tier patches awaiting operator approve/reject) | **DISTINCT** | Outcomes = look-back metrics; Reviews = decision queue. Keep both. |
| 15 | Tab **Failure Console** | Tab **Disputes** | `/api/failure-modes/state` (operational failures: authority lost, GPS silent, no-show, factor decline) | `/api/factoring/disputes` (factor disputes + clawback events) | **DISTINCT** | Different domains (operations vs factoring AR). Keep both. |
| 16 | Tab **SOP Flow** (`_route_sopflow`) | Tab **SOP Cards** (`sop_playbook`) | `SopFlowView` (LIVE: per-load lifecycle kanban with stalled flags) | `SOPPlaybookView` (PRINTABLE NARRATIVE: two operating models, stage-by-stage prose) | **DISTINCT (related)** | Live execution surface vs printable reference doc. Keep both. (After #4 cleanup, SOP Flow only lives in the header pill, not the tab bar.) |
| 17 | Tab **Carriers DB** | Tab **Specialty Hauler Lists** | `CarriersView` (registry: all approved carriers, filter / open carrier card) | `HaulerCategoriesView` (4 specialty categories: hopper, livestock, tanker, protein -- outreach lists) | **DISTINCT** | Registered carriers vs prospect outreach. Keep both. |
| 18 | Tab **Factor Picker** (`factor_matrix`) | Tab **Factors** (`factor_api`) | `FactorMatrixView` (ranked PROVIDER REGISTRY + OCR NOA test + push) | `FactorAPIWiringView` (per-factor CREDENTIAL wiring + test connectivity) | **DISTINCT** | Picker = which factor to use; Factors = API credential setup. Keep both. (Possible rename: "Factor Connections" for `factor_api` to mirror "Loadboard Connections".) |
| 19 | Header **Calendar** pill | Tab **Renewals** | `/calendar` -> `PlatformCalendarView` (`/api/system/calendar/month`: 6x7 grid + federal holidays + CHL milestones) | `/api/_renewals/summary` (authority/insurance/bond/cargo expiration tracker) | **DISTINCT (related)** | Calendar = visual grid w/ milestones; Renewals = expiration list. Renewals milestones get added to Calendar but the views are not duplicates. Keep both. |
| 20 | Header **Drafts** pill | (no matching tab) | `/pending-docs` -> `PendingDocsView` (auto-generated draft invoices / NOAs on delivered loads) | n/a | **DISTINCT** | No duplicate. Header-only. Keep as-is. |
| 21 | Tab **Doc Review** (`doc_review`) | Tab **Compliance** (`compliance`) | `ComplianceTab.jsx` (`/api/compliance/pending-review` — carrier doc review queue, accept/reject) | `ComplianceView` in App.js (`/api/compliance/checklist` — FMCSA authority/insurance/bond checklist) | **DISTINCT (related)** | Doc Review = action queue (per-carrier docs); Compliance = self-checklist (broker authority state). Keep both. |

---

## 3. Removal recommendations (explicit)

Six removals, lowest-risk first:

1. **REMOVE tab `_route_sopflow`** (label "SOP Flow"). Reason: byte-identical destination to the Header `SOP Flow` pill (both `<a href="/sop-flow" target="_blank">`). Keep: Header `SOP Flow` pill.
2. **REMOVE tab `_route_prevet`** (label "Pre-Vet"). Reason: byte-identical destination to Header `Pre-Vet` pill. Keep: Header `Pre-Vet` pill.
3. **REMOVE tab `_route_scorecard`** (label "Shipper Scorecard"). Reason: byte-identical destination to Header `Scorecards` pill. Keep: Header `Scorecards` pill.
4. **REMOVE tab `_route_ops`** (label "Ops Console"). Reason: byte-identical destination to Header `Console` pill. Keep: Header `Console` pill + the new in-SPA Ops Console v2 tab.
5. **REMOVE tab `bol_search`** (label "BOL Search", added today). Reason: same `/api/documents/by_bol` data already lives in Ops Console v2's "BOL Quick Search" card; for ad-hoc "what load is this BOL?" lookups, the header global search bar handles it. Operator-stated rule: bias today's additions toward REMOVE. Keep: Ops Console v2 BOL card + header search bar.
6. **KEEP** (do not remove) tab `_route_failures` (label "Failure Console"). Reason: no header pill exists for it; removing it loses access. Either build a header pill later or leave the tab as the canonical entry.

**Optional rename (not a removal):** `factor_api` label "Factors" -> "Factor Connections" so it mirrors "Loadboard Connections" and is harder to confuse with "Factor Picker". Operator decision.

After 5 removals, tab count drops from 42 (owner) / 30 (non-owner) to **37 (owner) / 25 (non-owner)**.

---

## 4. Canonical tab layout (recommended, post-cleanup)

Organized by category. Header pills shown at top; in-app tabs by category. Owner-only marked `(O)`.

### Header pill cluster (always visible, right side of header)
- Calendar (`/calendar`)
- Drafts (`/pending-docs`)
- Scorecards (`/shipper-scorecard`)
- Console (`/ops`)
- SOP Flow (`/sop-flow`)
- Pre-Vet (`/prevet`)
- Email dropdown (Create / View -> Inbox tab)
- Logout

### Tab nav (in-app, ordered by category)

**OPERATIONS**
1. Dashboard (`dashboard`)
2. Day-1 Monitor (`day1_dashboard`) (O)
3. Monitoring (`monitoring`)
4. Load Board (`loads`)
5. AI Matching (`matches`)
6. Live Map (`live_map`)
7. Auto Rules (`rules`)
8. Pipeline (`pipeline_walkthrough`) (O)
9. SOP Cards (`sop_playbook`)
10. Queues (`operator_queues`)

**FINDING WORK**
11. Find Loads (`aggregator`)
12. Market Radar (`market_radar`)
13. Market Rates (`rates`)
14. Loadboard Connections (`boards`)

**CARRIER MGMT**
15. Carriers DB (`carriers`)
16. Specialty Hauler Lists (`bulk_hunter`)

**DOCUMENT / BOL**
17. Doc Review (`doc_review`)

**FINANCIAL**
18. Accounting (`accounting`)
19. Factors (`factor_api`) (consider rename "Factor Connections")
20. Factor Picker (`factor_matrix`) (O)
21. Disputes (`disputes`) (O)

**COMMS**
22. Command Phone (`command`)
23. Inbox (`inbox`)
24. Notif Log (`notifications`) (O)

**MANUAL OPS**
25. Ops Console v2 (`ops_console_new`) (O)

**SELF-HEAL / DIAGNOSTICS**
26. Failure Console (`_route_failures`) — note: still a `_route_*` tab; promote to header pill or leave as tab (operator decision)
27. Self-Heal (`self_heal`) (O)
28. Patch Proposals (`patch_proposals`) (O)
29. Reviews (`dev_reviews`) (O)
30. Outcomes (`outcomes`) (O)
31. Alerts (`urgent_alerts`) (O)
32. AEther (`aether`)

**GOVERNANCE / SETUP**
33. Compliance (`compliance`)
34. Renewals (`renewals`) (O)
35. Vault (`vault`)
36. Owner (`owner`) (O)
37. Settings (`settings`)

Owner-only count: 12. Non-owner count: 25.

### Removed (do not re-add without operator decision)
- `_route_sopflow` (duplicate of header SOP Flow)
- `_route_prevet` (duplicate of header Pre-Vet)
- `_route_scorecard` (duplicate of header Scorecards)
- `_route_ops` (duplicate of header Console; functionality preserved via Ops Console v2 + header Console)
- `bol_search` (subsumed by Ops Console v2 + global search bar)

### Hidden-intentional (kept in code, not in nav — do not surface)
- `payments` (line 2937, commented out; carrier-payment workflow factored via RTS/OTR/Apex/Triumph, no direct CHL checks)
- `settlements`, `employees`, `facility`, `tracking`, `external`, `carrier_hub`, `automations`, `autodispatch`, `god_view` — all intentionally not in top-bar per various iter cleanups (see `HIDDEN-INTENTIONAL` markers throughout App.js)

---

## 5. Notes for the next agent

- The header pill cluster is the **canonical** entry-point for the five `/route` views. The `_route_*` tabs were Iter 135.13's "promote to top-bar" experiment; in practice both surfaces exist concurrently because the promotion didn't remove the original pills. Pick one: header pills are visually distinctive (colored), so keep them; remove the duplicating tabs.
- `ops_console_new` (Ops Console v2) is the **only** way to drive the manual workflow endpoints via Playwright -- removing it would block test automation. Keep.
- `BolSearchView.jsx` source file should remain in `frontend/src/views/` after the tab is removed, in case the operator wants to re-promote it later. Just unwire the tab nav entry + the activeTab block at line 3145.
- Five removals affect only `App.js` (tab nav array + activeTab render blocks). No backend changes. No new endpoints. No risk to data.
- Global search bar (`LoadDossierSearchBar`) accepts BOL number as input but returns LOADS, not documents. If operator wants the bar to ALSO return documents when input matches a BOL pattern, that's a separate enhancement (extend `/api/load-dossier/search` to merge in `/api/documents/by_bol` hits). Out of scope for this cleanup.
